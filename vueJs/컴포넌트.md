## 컴포넌트
- 대부분의 프론트엔드 프레임워크는 컴포넌트 기반
- 화면의 영역을 구분하여 코드로 관리하는 것
- ex. header, side 등
- 재사용성이 핵심

### 전역 컴퍼넌트
Vue.component('컴포넌트 이름', 컴포넌트 내용);

### 지역 컴퍼넌트
new Vue({
    components: {
        '컴포넌트 이름' : 컴포넌트 내용
    }
});

### 전역과 지역

### 상위 컴포넌트와 하위 컴포넌트 데이터 공유
- 상위 -> 하위 : prop
- 하위 -> 상위 : event