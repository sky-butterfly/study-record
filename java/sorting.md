## sorting

## sort()
- Arrays.sort(정렬 원하는 배열);
```
 int[] arr = [1, 2, 3];
 Arrays.sort(arr);
```

## ArrayList 정렬
- 아직 테스트 해보지 않음
- Collection.sort(array);
- Collection.sort(array, Collection.reverseOrder()); 