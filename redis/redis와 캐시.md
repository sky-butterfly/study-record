## Redis 와 캐시

### Look Aside (= Lazy Loading)
- 캐시를 옆에 두고 필요할 때만 데이터를 캐시에 로드하는 캐싱 전략
- 캐시에 데이터를 요청하고, 없을 경우 데이터베이스에서 데이터를 요청하고 이후 이 데이터를 다시 레디스에 저장
- 실제 사용되는 데이터만 캐시됨
- 레디스의 장애가 앱에 치명적인 영향을 주지 않음
- 캐시에 없는 데이터 조회 시 시간이 오래 걸림
- 캐시가 최신 데이터를 가지고 있다고 보장 할 수 없음 (데이터베이스에서 변경된 데이터는 캐시가 그 값을 알지 못함)

